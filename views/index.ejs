<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Url shortner</title>
  </head>
  <body>
    <h1>URL shortner</h1>

    <% if (locals.error) { %>
    <p class="error"><%= error %></p>
    <% } %>

    <form action="/url" method="post">
      <input type="url" name="url" id="url" placeholder="Long Url here..." />
      <input type="submit" value="shorten url" />
    </form>

    <div class="short-url">
      <% if (locals.short_url) { %>
      <p><%= short_url %> </p>
      <% } %>
    </div>
  </body>
</html>